@model List<UsuarioModel>
@*se le declara el tipo de dato que se le manda a la vista con arroba.model*@

@foreach (var item in Model)
{
    <p>@item.RUT</p>
    <p>@item.Nombre</p>

}


@*@model IList<ClientesContablesModel>
    @{
        ViewBag.Title = "Seleccionar Cliente Contable";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
    @using Microsoft.AspNet.Identityasdsadasdsadsadasdsadasd


    <p class="text-muted m-b-30 "></p>

    <div class="container">asdasdasd
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8">

                <p class="text-muted m-b-10"></p>
                <div class="panel panel-default  text-center slider-bg">
                    <div class="panel-body p-0">
                        <div class="">
                            <div id="owl-slider" class="owl-carousel">
                                <div class="item">
                                    <h1 class="text-wrap font-600">Selecciona tu Cliente Contable</h1>

                                    <p class="small">Derechos Reservados, 2018</p>
                                    @if (Model.Count() > 0)
                                    {
                                        <a href="@Url.Action("NuevoClienteContable","Contabilidad")" class="btn btn-success btn-small pull-right waves-effect waves-light" data-overlaySpeed="200" data-overlayColor="#36404a">
                                            <span class="btn-label"><i class="ion-android-add"></i></span>
                                            Clientes Contables
                                        </a>
                                        <br />
                                    }


                                    @if (Model.Count() == 0)
                                    {
                                        <br />
                                        <div class="alert alert-danger" role="alert">
                                            No se encontraron clientes contables
                                        </div>
                                        if (ViewBag.PerfilUsuario != null)
                                        {
                                            if (ViewBag.PerfilUsuario == "Admin" || ViewBag.PerfilUsuario == "SuperAdmin")
                                            {
                                                <a class="btn btn-primary" href="@Url.Action("NuevoClienteContable","Contabilidad")"><span class="btn-label"><i class="ion-android-add"></i></span>Crea clientes contables pinchando aquí</a>
                                            }
                                        }
                                    }
                                </div>
                                @if (Model.Count() != 0)
                                {
                                    <div class="item m-t-40">
                                        <p class="text-muted m-b-20 "></p>
                                        <table width="100%" class="tablesaw m-t-20 table m-b-0 table-bordered panel-color panel-primary table-responsive" id="tablaCliente">
                                            <thead>
                                                <tr>
                                                    <th class="text-center text-white">Rut Cliente</th>
                                                    <th class="text-center text-white">Razón Social</th>
                                                    <th class="text-center text-white">Seleccionar</th>

                                                </tr>
                                            </thead>
                                            <tbody>



                                                @foreach (var ClienteContable in Model)
                                                {
                                                    <tr id="@ClienteContable.ClientesContablesModelID">
                                                        <td>@ClienteContable.RUTEmpresa</td>
                                                        <td>@ClienteContable.RazonSocial</td>
                                                        <td><a href="javascript:SelecCliente('@ClienteContable.ClientesContablesModelID');"><span class="label label-table label-primary">Seleccionar</span></a></td>

                                                    </tr>
                                                }

                                            </tbody>
                                        </table>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>

            </div>

        </div>
        <div class="col-md-2"></div>
    </div>

    <style>

        .label-primary:hover {
            background-color: #00ffff;
        }

        #barralateral {
            display: none;
        }
    </style>

    <script>
        function SelecCliente(idClienteContable) {
            $.ajax({
                url: "DoSeleccionarClienteContable?EmisorID=" + idClienteContable,
                success: function (response) {
                    if (response != null && response.success) {
                        //alert(document.location.href);
                        document.location.href = "/Contabilidad/PanelClienteContable";
                    }
                    else {
                        alert("No se puede operar con este cliente contable");
                    }
                }
            });
        }
    </script>*@
